[id='adding-credentials_keystore_{context}']
= Adding credentials keystore

[role="_abstract"]
Create {brandname} credentials keystore to avoid
writing password in clear in the server config file. See <<Enabling TLS encryption>> for a use case.

.Procedure

. Create credentials keystore by specifying a `credentials add` command in the `deploy.security.batch` field.
+
.Store a password in a keystore
[source,yaml,options="nowrap",subs=attributes+]
----
include::yaml/pass_keystore_batch.yaml[]
----

. Credentials keystore needs then to be added to the server configuration.
+
.Configure a keystore
[source,yaml,options="nowrap",subs=attributes+]
----
include::yaml/configure_keystore.yaml[]
----
