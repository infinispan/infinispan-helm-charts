[id='adding-credentials_{context}']
= Adding custom user credentials

[role="_abstract"]
Add custom credentials to {brandname} clusters.

.Procedure

. Open the values file for your {brandname} cluster.
. Specify a `user create` command for your credentials as the value of the `deploy.security.batch` field.
+
The following example creates a user named "user1" that has the `deployer` role:
+
[source,yaml,options="nowrap",subs=attributes+]
----
include::yaml/authentication_batch.yaml[]
----
+
. Save and close your values file.
. Install the Helm chart to deploy your {brandname} cluster.
